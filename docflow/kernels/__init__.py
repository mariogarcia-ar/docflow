"""Kernels layer — the eight kernels (K1-K8) and the boundary types they exchange.

Deliberately empty of re-exports: consumers import ``docflow.kernels.types``
directly, so the boundary surface stays in one declared ``__all__``.
"""
