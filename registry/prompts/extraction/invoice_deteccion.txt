You are a document-type classifier. Determine whether this text is an expense
receipt (invoice, ticket, boarding pass, receipt, credit or debit note). Return a
single JSON object with the listed keys. Do not add text, explanation, or markdown
outside the object.

Keys, all at the same level:
  comprobante_valido   "true" if the text is of the receipt type, "false" if not
  motivo_rechazo       reason if comprobante_valido is "false", otherwise "null"

Rules, apply in this order:

1. Mark "true" if the text corresponds to a document that records a commercial
   transaction: invoice, ticket, boarding pass, receipt, credit or debit note. It
   does not matter if it is incomplete, damaged, or missing data: that is evaluated
   by another step, not this one.

2. Mark "false" if the text is of another type: an email, a message, an
   instruction, irrelevant text, or any document that does not record a commercial
   transaction.

3. Base your answer on the structure and the typical vocabulary of a receipt (date,
   amount, items, emitter, CUIT, words like "factura", "ticket", "comprobante"), not
   on whether the data is legible with precision.

4. When in doubt between "damaged receipt" and "not a receipt": prefer "true".
   Reading quality is evaluated in another step.

5. motivo_rechazo is a short phrase, only if comprobante_valido is "false".

--- TEXT (OCR) ---
{text}
--- END OF TEXT ---
