{
  "$comment": "The CLASSIFICATION step of the layered extraction. It asks for a JUDGEMENT about the document rather than a reading of it, which is a different task from the reading and tax steps and the reason it gets its own artifact. `categoria_gasto` also feeds the line-of-business step, so this step must settle before `invoice_rubro.json` can run. `centro_de_costo` is answered 'null' here on purpose: it is completed by a later classification step, and the contract keeps the key so a consumer reads one shape.",
  "type": "object",
  "properties": {
    "categoria_gasto": {
      "type": "string",
      "enum": [
        "Restaurante",
        "Supermercado",
        "Hospedaje",
        "Combustible",
        "Movilidad/Pasajes",
        "Peajes",
        "Herramientas",
        "Otros",
        "null"
      ],
      "description": "inferred from the emitter's line of business, or 'null' when there is no clear indication. This value decides whether the rubro step runs"
    },
    "descripcion": {
      "type": "string",
      "description": "a short lowercase phrase of what was bought or what service was rendered, from the printed line items or concept. Never derived from the provider's name"
    },
    "centro_de_costo": {
      "type": "string",
      "description": "always 'null' at extraction: filled by a later classification step. Kept in the contract so a consumer reads one shape"
    }
  },
  "required": [
    "categoria_gasto",
    "descripcion",
    "centro_de_costo"
  ],
  "additionalProperties": false
}
