"""OCR helpers: composition glue of the processor's own stages.

Helpers sequence primitives and utilities and build the published representations; they
never reach an engine and never decide whether the extraction is good enough to use. Phase
1 populates this package as the entry point is composed.
"""
