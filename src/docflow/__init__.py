"""``docflow`` — library-first document extraction, organized in layers.

Owned by the Kernels layer in PoC (``docs/artifacts/wbs.md`` §8). This marker
exists so the package is importable from the repository root.
"""
