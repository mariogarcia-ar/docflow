"""Image utilities: engine-independent helpers shared inside the processor.

Nothing here reaches an engine directly — a utility that needs OpenCV calls the matching
:mod:`docflow.image.primitives` function. Phase 1 populates this package as the
primitives land.
"""
